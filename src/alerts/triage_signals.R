#' Triage signals generated automatically
#'
#' Go through all of the automatically generated signals stored in
#' `output/{indicator_id}/signals.parquet` files. For all unique campaign in the
#' data, the `campaign_url` is previewed. For each campaign, the user can select
#' whether
